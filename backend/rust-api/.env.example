# Пример конфигурации Rust API
# Rust API автоматически загружает ../../.env через dotenvy
# Этот файл показывает опциональные локальные переопределения для разработки

# Опционально: переопределить уровень логирования для локальной разработки
# RUST_LOG=trainingground_api=debug,tower_http=debug

# Опционально: переопределить URL Python API для локального тестирования
# PYTHON_API_URL=http://localhost:8000

# Все секреты (MONGO_URI, REDIS_URI, JWT_SECRET) берутся из корневого .env
# Сгенерировать пароли: bash ../../infra/scripts/generate_secrets.sh
